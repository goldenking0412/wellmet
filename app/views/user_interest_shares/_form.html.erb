<%= form_for(@user_interest_share) do |f| %>
  <% if @user_interest_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_interest_share.errors.count, "error") %> prohibited this user_interest_share from being saved:</h2>

      <ul>
      <% @user_interest_share.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
