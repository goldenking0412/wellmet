<%= form_for(@error) do |f| %>
  <% if @error.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@error.errors.count, "error") %> prohibited this error from being saved:</h2>

      <ul>
      <% @error.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :not_found %><br>
    <%= f.text_field :not_found %>
  </div>
  <div class="field">
    <%= f.label :internal_server_error %><br>
    <%= f.text_field :internal_server_error %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
